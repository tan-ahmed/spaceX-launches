export const API_URL = 'https://api.spacexdata.com';
export const LAUNCHES_URL = `/v5/launches/query`;
export const PAYLOADS_URL = `/v4/payloads/query`;
export const CORES_URL = `/v4/cores/query`;

export enum Keys {
  launchQuery = 'launchQuery',
}
